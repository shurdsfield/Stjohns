Fetch the latest player stats and match results from the Pitchero website, then rebuild the U13s site.
Run this from your local Mac (Pitchero is blocked in the cloud sandbox):

```bash
./build.sh u13s --fetch-stats
```
